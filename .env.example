# ===========================================
# INNOV'EVENTS MANAGER - Variables d'environnement
# ===========================================
# Copiez ce fichier vers .env et remplissez les valeurs
# cp .env.example .env
# ===========================================

# ------------ Application ------------
NODE_ENV=development
PORT=3000
FRONTEND_URL=http://localhost:5173

# ------------ PostgreSQL ------------
# Ne pas modifier pour Docker (sauf les mots de passe)
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=innovevents
POSTGRES_PASSWORD=innovevents123
POSTGRES_DB=innovevents_db

# ------------ pgAdmin (Interface PostgreSQL) ------------
PGADMIN_EMAIL=admin@admin.com
PGADMIN_PASSWORD=admin123

# ------------ MongoDB ------------
# Ne pas modifier pour Docker (sauf les mots de passe)
MONGO_HOST=mongodb
MONGO_PORT=27017
MONGO_USER=innovevents
MONGO_PASSWORD=innovevents123
MONGO_DB=innovevents_logs

# ------------ Mongo Express (Interface MongoDB) ------------
MONGO_EXPRESS_USER=admin
MONGO_EXPRESS_PASSWORD=admin123

# ------------ JWT (Authentification) ------------
# IMPORTANT : Changez ces valeurs en production !
JWT_SECRET=changez_moi_avec_une_chaine_secrete_de_64_caracteres_minimum
JWT_EXPIRES_IN=24h
JWT_REFRESH_SECRET=changez_moi_aussi_autre_chaine_secrete_64_caracteres
JWT_REFRESH_EXPIRES_IN=7d

# ------------ Email SMTP (Optionnel) ------------
# Pour Gmail : créez un "App Password" sur https://myaccount.google.com/apppasswords
# Laissez vide si vous ne voulez pas envoyer d'emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=votre_email@gmail.com
SMTP_PASS=votre_app_password_gmail
SMTP_FROM=contact@innovevents.com
SMTP_FROM_NAME=Innov'Events

# ------------ Cloudinary (Stockage images - Optionnel) ------------
# Créez un compte gratuit sur https://cloudinary.com
# Laissez vide pour utiliser le stockage local (images perdues au redémarrage Docker)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
